{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "test",
            "type": "lua",
            "request": "launch",
            "program": "${workspaceRoot}/make.lua",
            "stopOnEntry": true,
            "cwd": "${workspaceRoot}",
            "path": "${workspaceRoot}/?.lua;${workspaceRoot}/luabuild/?.lua",
            "cpath": "${workspaceRoot}/luabuild/?.dll",
            "arg": [
                "Debug"
            ],
            "env": {
                "PATH": "${workspaceRoot}/luabuild/;${env:PATH}"
            },
            "consoleCoding": "ansi"
        }
    ]
}